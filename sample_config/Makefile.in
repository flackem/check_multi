#---
#--- Makefile sample-config
#---
#--- $Id$
#---
prefix=@prefix@
LIBEXECDIR=@LIBEXECDIR@
HTMLDIR=@datarootdir@
INSTALL=@INSTALL@
INSTALL_OPTS=@INSTALL_OPTS@

all: 

clean:
	cd cluster    && $(MAKE) $@
	cd multi_long && $(MAKE) $@
	cd nagiostats && $(MAKE) $@
	cd start_process && $(MAKE) $@
	cd network_interface && $(MAKE) $@
	

distclean: clean
	cd cluster    && $(MAKE) $@
	cd multi_long && $(MAKE) $@
	cd nagiostats && $(MAKE) $@
	cd start_process && $(MAKE) $@
	cd network_interface && $(MAKE) $@
	-rm -f Makefile

devclean: distclean

install-config: 
	cd multi_long && $(MAKE) $@
	cd cluster    && $(MAKE) $@
	cd nagiostats && $(MAKE) $@
	cd start_process && $(MAKE) $@
	cd network_interface && $(MAKE) $@

